%%
%INTERPOLATING SCHEDULES
%%

\begin{figure}[!ht]
  \centering
  \begin{tikzpicture}
    \begin{axis}[name=plot,
    domain=0:1, restrict y to domain=0:1,
    xmin=0, xmax=1,
    ymin=0, ymax=1,
    width = 80mm,
    samples=100
    ]

    \addplot[color=black]{(0.5)*((2*x-1)^3+1)};

    \end{axis}
  \end{tikzpicture}

  \caption{\textbf{Non-linear interpolating schedule \bm{$s_{NL}(t/T)$}} These figures show the difference between the original interpolating schedule derived by Roland and Cerf for the unstructured search (left) and our $s_{NL}(t/T)$(right). Although quite different we study the effects of this particular non-linear interpolating schedule and compare it with the linear one used by Farhi and Gutmann \cite{Farhi2000}.}
  \label{fig:interpolating_schedule_compared}
\end{figure}
