%%
%TIME INDEPENDENT: PROBABILITY FOR SAMPLED GAMMA
%%

%ITERS I DISTRIBUTION FOR INCREASING LOWER BOUND TIME

\begin{figure}[ht]
\centering
  \begin{tikzpicture}
    \begin{axis}[name=plot,
    xmin=0, xmax=1000,
    ymin=0, ymax=0.14,
    width = 90mm,
    height= 70mm,
    xlabel = $\bm{T}$,
    ylabel = $\bm{p}$,
    legend style={at={(0.95,0.05)},
    anchor=south east}]
    \addplot[color=rossoscuro,no marks, thick, x=a, y=b] table{./Data/fig2/51_gamma03.txt};\addlegendentry{$\gamma=0.35$}
    \addplot[color=rossoscuro,dashed, no marks, thick, x=a, y=b] table{./Data/fig2/51_gamma055.txt};\addlegendentry{$\gamma=0.55$}
    \addplot[color=rossoscuro,dotted, no marks, thick, x=a, y=b] table{./Data/fig2/51_gamma085.txt};\addlegendentry{$\gamma=0.85$}

    \end{axis}
  \end{tikzpicture}
  \caption{\textbf{Probability for a $\bm{Cy(51)}$ with sampled $\bm{\gamma}$}: The figure shows the probability for the cycle graph $Cy(51)$, evaluated with the time-independent Hamiltonian and a few sampled $\gamma$. We can see that the probability does not increase significantly with time, therefore we can focus our analysis on $T\leq N$.}
  \label{fig:time_independent_sampled_gamma}
\end{figure}
